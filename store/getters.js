const getters = {
  // token: state => state.user.token,
  // name: state => state.user.name,
  listStaff: state => state.staff.list,
  listGuest: state => state.guest.list,
    listDepartment: state => state.department.list

};
export default getters;
